{% extends 'back_base.html.twig' %}

{% block title %}Éditer Image{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="row ">
        <div class="col-lg-12">  
            <div class="row ">
                <div class='test col-12'>
                    <h1>Éditer Image</h1>
                    {{ include('back/uploads/picture/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
                    {{ include('back/uploads/picture/_delete_form.html.twig') }}
                    <a href="{{ path('uploads_index') }}">Revenir à la liste</a>
                </div>
            </div>
        </div>
    </div>      
</div>    

{% endblock %}
