{% extends 'base.html.twig' %}

{% block title %}Edit File{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="row ">
        <div class="col-lg-12">  
            <div class="row ">
                <div class='test col-12'>
                    <h1>Éditer Fichier</h1>
                    {{ include('back/uploads/file/_form.html.twig', {'button_label': 'Update'}) }}
                    {{ include('back/uploads/file/_delete_form.html.twig') }}
                    <a href="{{ path('uploads_index') }}">Revenir à la liste</a>
                </div>
            </div>
        </div>
    </div>      
</div>   
{% endblock %}
