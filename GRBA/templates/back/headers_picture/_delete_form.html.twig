<form method="post" action="{{ path('headers_picture_delete', {'id': headers_picture.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ headers_picture.id) }}">
    <button class="btn">Delete</button>
</form>
