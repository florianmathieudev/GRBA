{% extends 'base.html.twig' %}

{% block title %}HeadersPicture{% endblock %}

{% block body %}
    <h1>HeadersPicture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ headers_picture.id }}</td>
            </tr>
            <tr>
                <th>Path</th>
                <td>{{ headers_picture.path }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ headers_picture.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('headers_picture_index') }}">back to list</a>

    <a href="{{ path('headers_picture_edit', {'id': headers_picture.id}) }}">edit</a>

    {{ include('headers_picture/_delete_form.html.twig') }}
{% endblock %}
