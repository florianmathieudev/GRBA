{% extends 'back_base.html.twig' %}

{% block title %}Éditer Horaire{% endblock %}

{% block body %}
    <h1>Éditer Horaire</h1>

    {{ include('back/horaire/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('horaire_index') }}">Revenir à la liste</a>

    {{ include('backhoraire/_delete_form.html.twig') }}
{% endblock %}
