{% extends 'base.html.twig' %}

{% block title %}TypesPicture{% endblock %}

{% block body %}
    <h1>TypesPicture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ types_picture.id }}</td>
            </tr>
            <tr>
                <th>Path</th>
                <td>{{ types_picture.path }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ types_picture.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('types_picture_index') }}">back to list</a>

    <a href="{{ path('types_picture_edit', {'id': types_picture.id}) }}">edit</a>

    {{ include('types_picture/_delete_form.html.twig') }}
{% endblock %}
