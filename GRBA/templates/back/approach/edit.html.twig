{% extends 'back_base.html.twig' %}

{% block title %}Éditer Contact{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="row ">
        <div class="col-lg-12">  
            <div class="row ">
                <div class='test col-12'>
                    <h1>Éditer Contact</h1>
                    {{ include('back/approach/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
                    {{ include('back/approach/_delete_form.html.twig') }}
                    <a href="{{ path('header_index') }}">Revenir a la liste</a>
                </div>
            </div>
        </div>
    </div>      
</div>    
{% endblock %}
