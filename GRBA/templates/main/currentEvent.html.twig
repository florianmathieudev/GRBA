{% extends 'base.html.twig' %}

{% block title %}GRBA - Groupe des Randonneurs et Baliseurs Aubois{% endblock %}

{% block body %}
    


    <div class="">
        <div class="row">
            {% for picture in pictures %}
            <img src="{{ asset('image/' ~ picture.path ) }}" height="150em" >
            {% endfor %}
        </div>
        <div>
                <h5>{{ event.place }}</h5>
                    <p>{{ event.date|localizeddate('none', 'none', 'none', null, 'EEEE dd MMMM')}}</p>
                    <p>{{ event.content|truncate(50) }}</p>
                    <p>{{ event.content }}</p>
            </div>
            
        </div>
<a href="{{ path('main') }}">Revenir à la page principale</a>

{% endblock %}